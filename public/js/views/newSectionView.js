// Generated by CoffeeScript 1.7.1
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.newSectionView = (function(_super) {
    __extends(newSectionView, _super);

    function newSectionView() {
      return newSectionView.__super__.constructor.apply(this, arguments);
    }

    newSectionView.prototype.tagName = 'div';

    newSectionView.prototype.template = _.template($('#section-create-template').html());

    newSectionView.prototype.data = {};

    newSectionView.prototype.events = {
      'change': 'change',
      'click button.save': 'saveSection',
      'click button.create': 'openCreate'
    };

    newSectionView.prototype.initialize = function() {
      this.render();
      return this;
    };

    newSectionView.prototype.render = function() {
      return this.$el.html(this.template());
    };

    newSectionView.prototype.change = function(event) {
      var changeTarget;
      console.log('changed');
      changeTarget = event.target;
      return this.data[changeTarget.name] = changeTarget.value;
    };

    newSectionView.prototype.openCreate = function() {
      var form;
      form = $('#create-form');
      form.show();
      $('#add-save-button').removeClass('btn-primary create');
      $('#add-save-button').addClass('btn-success save');
      return $('#add-save-button').html('Save new Section');
    };

    newSectionView.prototype.closeCreate = function() {
      var form;
      form = $('#create-form');
      form.hide();
      $('#add-save-button').removeClass('btn-success saveNew');
      $('#add-save-button').addClass('btn-primary create');
      return $('#add-save-button').html('Create new Section');
    };

    newSectionView.prototype.saveSection = function() {
      var newSec, self;
      console.log('saving...');
      newSec = new Section(this.data);
      self = this;
      newSec.save({}, {
        success: function() {
          console.log('saved');
          return self.data = {};
        },
        error: function() {
          return console.log('error');
        }
      });
      this.render();
      return self.trigger('save');
    };

    return newSectionView;

  })(Backbone.View);

}).call(this);

//# sourceMappingURL=newSectionView.map
