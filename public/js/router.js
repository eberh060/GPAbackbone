// Generated by CoffeeScript 1.7.1

/*
Created by mart2967 on 1/30/14.
 */

(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  window.AppRouter = (function(_super) {
    __extends(AppRouter, _super);

    function AppRouter() {
      return AppRouter.__super__.constructor.apply(this, arguments);
    }

    AppRouter.prototype.routes = {
      '': 'index'
    };

    AppRouter.prototype.index = function() {
      var blankSection, listView, sectionList;
      sectionList = new SectionCollection();
      blankSection = new newSectionView();
      listView = new SectionCollectionView({
        collection: sectionList
      });
      sectionList.fetch({
        success: function() {
          return listView.render();
        }
      });
      $('#bs-example-navbar-collapse-1').html(new NavbarView({
        collection: sectionList
      }).$el);
      $('#add-section-div').html(blankSection.$el);
      $('#content').html(listView.$el);
      return this.listenTo(blankSection, 'save', function() {
        console.log('detected save');
        return sectionList.fetch({
          success: function() {
            return listView.render();
          }
        });
      });
    };

    return AppRouter;

  })(Backbone.Router);

  $(document).ready(function() {
    var app;
    app = new AppRouter();
    return Backbone.history.start({
      pushState: true
    });
  });

}).call(this);

//# sourceMappingURL=router.map
